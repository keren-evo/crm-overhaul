# Meta Pixel (client-side — prefix with NEXT_PUBLIC_)
NEXT_PUBLIC_META_PIXEL_ID=

# CRM webhook URL (optional — enables POST /api/webhooks/crm forwarding)
CRM_WEBHOOK_URL=

# Comma-separated allowed CORS origins for API routes
CORS_ORIGINS=http://localhost:3000
